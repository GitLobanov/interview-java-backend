#### Задача 1

```java
/**
Реализовать очередь, которая будет реализовывать методы Queue и Collection.
Задать контракт для Queue и Collection нужно самостоятельно.
Описывать временную сложность в реализуемых методах.
Выбрать подход односвязного списка или динамического массива.
**/
```

#### Задача 2

Реализовать свой метод intern для кастомного StringPool.

#### Задача 3

```java

/*
Необходимо реализовать класс, который умеет принимать элементы
и в любой момент времени возвращать N(capacity) наибольших уникальных элементов.
Если количество элементов помещенных в класс меньше N - то требуется организовать ожидание, до тех пор пока не наберется N элементов
 */
public class TopNElementsCollection {

    public TopNElementsCollection(int capacity) {

    }

    public void push(int value) {

    }

    public List<Integer> pull() throws InterruptedException {

    }
}
```

#### Задача 4

```java
/**
Реализовать метод фильтрации списка
заказов по цене по дате по статусу.
Для реализации использовать лямбда выражение (использовать стримы),
в дальнейшим метод должен быть расширен (использовать паттерн стратегия)
можно использовать вложенные классы
**/
```